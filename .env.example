# Copy to .env and fill in. Only EXPO_PUBLIC_* vars are exposed to the client.
# Non-sensitive config only — never put secrets that must stay private here.
EXPO_PUBLIC_API_BASE_URL=https://api.mitro.app
EXPO_PUBLIC_API_TIMEOUT_MS=20000
EXPO_PUBLIC_APP_ENV=development
